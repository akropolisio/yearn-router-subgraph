{
  "name": "yearn-router-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "pre-build": "rm -rf ./generated && graph codegen",
    "deploy-studio": "yarn pre-build && graph deploy --studio --version-label ${VERSION} akropolis-yearn-router --deploy-key ${STUDIO_DEPLOY_KEY}"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "version": "0.0.1"
}
