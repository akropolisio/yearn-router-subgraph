{
  "name": "yearn-router-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ yearn-router-subgraph",
    "create-local": "graph create --node http://localhost:8020/ yearn-router-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ yearn-router-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 yearn-router-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
